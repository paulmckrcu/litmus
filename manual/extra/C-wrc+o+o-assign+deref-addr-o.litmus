C C-WRC+o+o-assign+deref-addr-o
{
x = b;
b = 42;
y = 3;
1:r0 = y;
}

P0(int *y)
{
	WRITE_ONCE(*y, 1);
}

P1(int *y, int *x)
{
	r3 = READ_ONCE(*y);
	rcu_assign_pointer(*x, r0);
}

P2(int *x)
{
	r1 = rcu_dereference(*x);
	r2 = READ_ONCE(*r1);
}

exists (1:r3=1 /\ 2:r1=y /\ 2:r2=3)
